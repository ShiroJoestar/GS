<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Reçu</title>
    <style>
        /* Styles for the receipt */
        body {
            font-family: Arial, sans-serif;
            font-size: 14px;
        }
        .receipt {
            max-width: 300px;
            margin: 0 auto;
            padding: 10px;
            border: 1px solid #000;
        }
        .header {
            text-align: center;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .info {
            margin-bottom: 10px;
        }
        .info strong {
            display: inline-block;
            width: 100px;
            font-weight: bold;
        }
        .items {
            margin-bottom: 10px;
        }
        .items table {
            width: 100%;
            border-collapse: collapse;
        }
        .items table th {
            text-align: left;
            border-bottom: 1px solid #000;
            padding: 5px 10px;
        }
        .items table td {
            border-bottom: 1px solid #ccc;
            padding: 5px 10px;
        }
        .total {
            text-align: right;
            margin-top: 10px;
        }
        .total strong {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="receipt">
        <div class="header">Reçu</div>
        <div class="info">
            <strong>Date:</strong>{{purchase.date|date("Y-m-d")}}<br>
            <strong>Order ID:</strong> 123456789<br>
            <strong>Client:</strong> {{purchase.client.name}}<br>
        </div>
        <div class="items">
            <table>
                <thead>
                    <tr>
                        <th>Produit</th>
                        <th>Prix</th>
                        <th>Quantité</th>
                    </tr>
                </thead>
                <tbody>
                {% for qt in quantityProduct %}
                    <tr>
                        <td>{{ qt.getProduct().0.name }}</td>
                        {% set num1 = qt.nombre %}
                        {% set num2 = qt.getProduct().0.price %}

                        <td>Ar {{ num1 * num2 }}</td>

                        
                        <td>{{ qt.nombre }}</td>
                    </tr>
                {% endfor %}
                    
                </tbody>
            </table>
        </div>
        <div class="total">
            <strong>Total:</strong> Ar {{purchase.total}}
        </div>
    </div>
</body>

<script>
    window.print();
</script>
</html>